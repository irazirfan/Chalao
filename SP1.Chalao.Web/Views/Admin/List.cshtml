@model SP1.Chalao.Framework.Objects.Result<List<SP1.Chalao.Entities.Admins>>
@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/_InnerLayout.cshtml";
}

<h2> Admins </h2>

<div class="row">
    <div class="col-md-8">
        <input type="text" placeholder="search" id="searchText" value="@Request.Params["key"]"/>
        <a onclick="SearchUrl('@Url.Action("List","Admin")')" class="btn btn-sm btn-success"><span class="glyphicon glyphicon-search">Search</span></a>
        <a href="@Url.Action("Edit","Admin", new {id=-1})" class="btn btn-sm btn-success"><span class="glyphicon glyphicon-plus">Add</span></a>
    </div>
    
    @if (SP1.Chalao.Framework.Helper.ValidationHelper.IsValidString(ViewBag.Error))
    {
        <div class="alert alert-danger alert-dismissable" role="alert">
            <button type="button" class="close" data-dissmiss="alert"><span aria-hidden="true"></span></button>
            <strong>Error!</strong> @Model.Message
        </div>
    }

    else

    {
        <table class="table table-striped table-hover">
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Email</th>
                <th>Mobile</th>
                <th>Joining Date</th>
                <th>Action</th>
            </tr>
            @foreach (var admin in Model.Data)
            {
                <tr>
                    <td>@admin.ID</td>
                    <td>@admin.Users.Name</td>
                    <td>@admin.Users.Email</td>
                    <td>@admin.Users.Mobile</td>
                    <td>@admin.JoinDate.ToString("dd/MM/yyyy")</td>
                    <td>
                        <a href="@Url.Action("Edit","Admin", new {id =admin.ID })" class="btn btn-sm btn-success"><span class="glyphicon glyphicon-edit">Edit</span></a>
                        <a onclick="DeleteUrl('@Url.Action("Delete","Admin", new {id = admin.ID})')" class="btn btn-sm btn-danger"><span class="glyphicon glyphicon-remove">Delete</span></a>
                    </td>
                </tr>
            }
        </table>
    }

</div>


@section Scripts
{
    <script>

        function SearchUrl(url) {
            window.location.href = url + "?key=" + $('#searchText').val();
        }

        function DeleteUrl(url) {
            if (confirm("Are You Sure?"))
                window.location.href = url;
        }

    </script>
}

